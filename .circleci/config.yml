version: 2.1

orbs:
  node: circleci/node@4.7

jobs:
  build:
    executor: node/default
    steps:
      - checkout
      - node/install-packages
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Run tests
          command: npm test
